# HW-123 four-corner YOLO Pose dataset.
# The four semantic points follow the physical PCB, not the camera frame.
path: ../datasets/hw-123-corner-pose-v1
train: images/train
val: images/val
test: images/test

kpt_shape: [4, 3]
names:
  0: hw-123
kpt_names:
  0:
    - pcb_TL
    - pcb_TR
    - pcb_BR
    - pcb_BL
