# MRD_TF240_8P_CS train-only robust augmentation dataset.
# Validation and test are copied unchanged from v1; only train is augmented.
path: ../datasets/mrd-tf240-8p-corner-pose-v2-robust
train: images/train
val: images/val
test: images/test

kpt_shape: [4, 3]
names:
  0: mrd-tf240-8p-cs
kpt_names:
  0:
    - mount_TL
    - mount_TR
    - mount_BR
    - mount_BL
