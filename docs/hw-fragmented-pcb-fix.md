# HW 深灰背景方向失敗修正（2026-09-11）

## 重播證據

使用 `runs/acceptance/2026-09-11/S02-HW-dark-after-confirmation-fix-01/failure-003-source.jpg` 重新推論：confidence 0.4437、模型框約 (919,431)-(1076,612)，角點歪斜。舊方向檢查及 PCB 重建均回傳 None。

原藍色遮罩混入低飽和灰背景雜訊；板上亮色元件又將藍色 PCB 切成多段，單一連通輪廓無法代表完整板體。以較高飽和藍色、3×3 開運算取得碎片集合，可重建約 (954,455)-(1060,596) 的 PCB。

## 修改

- 只對 HW、只在本次模型框內、原方向／邊界重建失敗時增加碎片重建。
- 要求高飽和藍色支持、尺寸、面積、比例及覆蓋率；再經原有全列焊盤對比與方向差距門檻。
- 新回退另要求焊盤對側兩個安裝孔的暗色圓形證據。沒有板框、無焊盤或缺孔不接受，不更動零件 scope 或 Pin 定義。
- 第一版重啟現場 213 幀全 searching，其中 197 方向未確認、16 等待共識；HW 擺位與之前不同。新照片重播顯示安裝孔透出較亮桌面，因此補上局部孔緣圓形檢測，要求兩孔高度及半徑一致，不只依賴暗色填充。
- 不改 HC、TFT 或 Pi 的判定程式，不增加使用者確認步驟。

## 驗證邊界

### 透視修正與真正來源影格（後續）

先前兩張顯示失敗照片重播皆通過，不能代表原模型失敗影格。新增按需 `/api/tracking/component-source` 回傳 PNG 無損來源及相同 frame_id 的 component_pose；方向失敗保存當時輸入角點與模型框。平時不增加相機顯示編碼。

`HW-source-failures/1..3` 抓到傾斜 PCB：minAreaRect 將透視四邊形拉成矩形，焊盤取樣錯位。改用藍色碎片凸包的四邊形近似（凸、四點時），否則保留原矩形回退。沒有放寬焊盤與兩孔驗證門檻。三份原始來源／角點重播及既有案例通過。

901 項完整測試通過；重啟 PID 49328。現場 `S02-HW-perspective-fix-live-01` 10.016 秒 220/220 HW locked，其他類全 searching。取樣前 UI 曾有 TFT 虛線誤框，列入待修，不能用隨後取樣正常結案。現場位置曾改變，非嚴格固定影像 A/B；HW 此輪靜態達標不代表多次重新放置或完整跨背景通過。

新照片四個旋轉、灰色負樣本與遮住安裝孔測試通過；既有 HW 保存圖回歸一併通過。這只是保存圖與合成旋轉的回歸，不代表現場任意角度或完全遮擋可辨識。上游當時的推論影格較保存顯示影格舊；這裡使用保存照片重新推論，並非還原原上游同幀。
# 模型框裁邊補充（2026-09-11）

`HW-return-source-failures/1..3` 的同步模型原圖暴露模型框裁掉 PCB 邊界的問題。新增有限擴邊重試：原 ROI 優先，失敗後才增加短邊 10% 邊距，仍須通過完整焊盤列、外形及雙固定孔檢查。直接對所有案例擴邊曾造成 7 個舊案例失敗，因此未採用該版本。

回歸涵蓋兩批共六份同步來源影格，完整測試 904 passed（1 個既有 Starlette warning）。重啟後 `S02-HW-box-margin-live-01` 原位 10 秒 211/219 locked（96.35%），8 次仍為方向未確認；其他模組全 searching。這只是靜態樣本，放回重複性、誤框及實體 Pin 精度仍待驗收。
